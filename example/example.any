function resultOf(a, b) {
    return { 
        a: a, 
        b: b - 1 - a, 
        c: log(a) + log(b)
    };
}

def calculate(x) {
    int a = 1;
    int b = 1337;
    return resultOf(a * x, b + 1);
}

fn main(int x) {
   res = calculate(x)
   print(res)
}

main(1);